# Claude Code IDE settings
.claude/

# Backups
*.bak
*.old
*.tmp

# OS files
Thumbs.db
.DS_Store

# Development artifacts
WithClaude/
OLD/

# Les zips de release vivent dans GitHub Releases, pas dans le depot : suivis,
# ils gonflent l'historique ET se retrouvent empaquetes dans l'archive de la
# release suivante (git archive embarque tout ce qui est suivi — sur BagsEnh la
# v2.3.1 contenait ainsi la v2.3.0, 80 Ko inutiles pour les joueurs).
*.zip
